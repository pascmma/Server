const sql = require('mssql');
/**
 * 
 * const [user,setUser] = React.useState([]);
 * 
 * 
 * async componentDidMOunt{
    const res = await axios.get('http://localhost:4000/api/users')  // para poder comunicarme con el servidor(backend)
    console.log()    // los usuarios los obtendra de la base de datos
    setUser[user: res.data
    
 }
 */


//obtener el json que nos pasará el login, 
/*
  
 */

